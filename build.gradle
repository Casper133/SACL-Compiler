plugins {
    id "org.jetbrains.kotlin.jvm" version "$kotlinVersion"
}

allprojects {
    apply plugin: "org.jetbrains.kotlin.jvm"

    repositories {
        jcenter()
    }

    dependencies {
    }
}
